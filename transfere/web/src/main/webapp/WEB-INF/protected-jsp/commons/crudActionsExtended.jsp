<c:if test="${now['buttons']['transferir']}">
<v:action labelKey="label.transfere.transferir" helpKey="help.transfere.transferir" elementId="Transferir" icon="transferir" iconClass="Transferir" action="transferir" hotKey="Ctrl+Shift+t" />
</c:if>
<c:if test="${now['buttons']['publicar']}">
<v:action labelKey="label.transfere.publicar" helpKey="help.transfere.publicar" elementId="Publicar" icon="publicar" iconClass="Publicar" action="publicar" hotKey="Ctrl+Shift+p" />
</c:if>