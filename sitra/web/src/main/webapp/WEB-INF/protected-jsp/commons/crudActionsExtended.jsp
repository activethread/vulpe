<c:if test="${now['buttons']['transferir']}">
<v:action labelKey="label.sitra.transferir" helpKey="help.sitra.transferir" elementId="Transferir" icon="transferir" iconClass="Transferir" action="transferir" hotKey="Ctrl+Shift+t" />
</c:if>
<c:if test="${now['buttons']['publicar']}">
<v:action labelKey="label.sitra.publicar" helpKey="help.sitra.publicar" elementId="Publicar" icon="publicar" iconClass="Publicar" action="publicar" hotKey="Ctrl+Shift+p" />
</c:if>